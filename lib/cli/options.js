const { createOptions } = require('./options/create');
const { destroyOptions } = require('./options/destroy');
const { statusOptions } = require('./options/status');

module.exports = {
    createOptions,
    destroyOptions,
    statusOptions,
};
